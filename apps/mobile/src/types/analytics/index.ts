/**
 * Analytics Type Definitions
 * 
 * Re-export types for external use.
 * Components should import types from here.
 */

export type {
  AnalyticsArea,
  EventName,
  BaseEventProperties,
  EventSchema,
  EventProperties,
} from '../../lib/analytics/schema';
